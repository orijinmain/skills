# Full Mode

Use standard adaptive routing. The orchestrator handles trivial work directly, delegates bounded discovery or implementation by role, parallelizes independent reads when useful, and applies verification gates according to risk and user-visible impact. This is the default mode.
