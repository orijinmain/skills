# Lite Mode

Use a high delegation threshold. The orchestrator handles small and ordinary tasks directly. Delegate only clearly bounded work with an obvious specialization or context-isolation benefit. Prefer serial execution, one worker at a time, and optional independent review only for meaningful risk.
